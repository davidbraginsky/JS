let mode = "normal"; // работает
let my_valu3 = 102; // работает
let 3my_value3 = "102"; // нельзя начинать имя переменной с цифры
let __hello__ = "world"; // работает
let $$$ = "money"; // работает, но не лучшая идея, т.к. по имени может быть не сразу понятно о чем идёт речь
let!0_world = true; // нельзя начинать имя перменной со спецсимвола (кроме $ и _)

